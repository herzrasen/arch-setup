- name: Install packages
  pacman:
    name:
      - bspwm
      - sxhkd
      - polybar
      - dunst
      - rofi
      - picom
    state: present

- name: Create .xinitrc
  template:
    src: ../templates/xinitrc.j2
    dest: "/home/{{ user }}/.xinitrc"
  become: yes
  become_user: "{{ user }}"
